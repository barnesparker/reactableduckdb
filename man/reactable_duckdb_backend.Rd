% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend.R
\name{reactable_duckdb_backend}
\alias{reactable_duckdb_backend}
\title{Create a DuckDB-backed server backend for reactable}
\usage{
reactable_duckdb_backend(
  data,
  key = NULL,
  filter_spec = NULL,
  max_page_size = 500L,
  cache_counts = TRUE,
  validate_key = FALSE
)
}
\arguments{
\item{data}{A lazy \code{duckplyr_df} (e.g. from
\code{\link[duckplyr:read_parquet_duckdb]{duckplyr::read_parquet_duckdb()}}, ideally with \code{prudence = "stingy"})
or a dbplyr lazy table backed by a DuckDB connection. Eager data frames
are refused.}

\item{key}{Optional name of a column with unique, orderable values, used
as a deterministic sort tie-breaker. Uniqueness is the caller's
responsibility unless \code{validate_key = TRUE}; a duplicated key still
yields nondeterministic order between pages.}

\item{filter_spec}{Optional named list of per-column filter overrides.
Each entry may contain \code{exact = TRUE} (text columns match exactly
instead of by substring) and/or \code{type} (pin one of \code{"text"},
\code{"numeric"}, \code{"date"}, \code{"datetime"}, \code{"logical"}).}

\item{max_page_size}{Hard upper bound on rows per page. Requests beyond
it are refused with an error, never clamped.}

\item{cache_counts}{Cache filtered row counts (bounded LRU, keyed by the
canonical filter set)? Paging and re-sorting reuse a cached count;
changing filters does not.}

\item{validate_key}{Run one scalar \verb{COUNT(*) = COUNT(DISTINCT key)} query
at construction to verify \code{key} is unique? Off by default because it
scans the source once.}
}
\value{
A \code{reactable_duckdb_backend} object, to be passed to
\code{\link[=reactable_duckdb]{reactable_duckdb()}}.
}
\description{
Wraps a lazy DuckDB query — a duckplyr data frame or a dbplyr
\code{tbl_sql}/\code{tbl_dbi} — as a server-side data backend for
\code{\link[=reactable_duckdb]{reactable_duckdb()}}. DuckDB performs all filtering, counting, sorting,
and pagination. Only three things are ever materialized into R: a
zero-row schema prototype (at construction), a scalar filtered count, and
the requested page of rows.
}
\details{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The backend holds a reference to the source's DuckDB connection but never
owns it: it will not reconnect, refresh, or disconnect. When the
underlying data, query, or schema changes (for example after a pin
refresh), the application must construct a new backend and re-render the
widget — a live backend cannot detect the change.
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE) && reactableduckdb:::has_reactable_backend_api()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
con <- DBI::dbConnect(duckdb::duckdb())
DBI::dbWriteTable(con, "flights", data.frame(id = 1:100, dist = rnorm(100)))
src <- dplyr::tbl(con, "flights")
backend <- reactable_duckdb_backend(src, key = "id")
backend
DBI::dbDisconnect(con, shutdown = TRUE)
\dontshow{\}) # examplesIf}
}
